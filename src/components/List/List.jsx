// Import the core React library, enabling us to define and use React components
import React from "react";

const List = () => {
          return(
                    <h1>List</h1>

          )
}

export default List 