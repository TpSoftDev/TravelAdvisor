// Import the React library to enable the use of React components and JSX syntax
import React from "react";

// Define the App component as a function
const App = () => {
  return (
    <div>
      <h1>Hello, World!</h1>
    </div>
  );
};

// Export the App component to use it in other files, such as index.js
export default App;
